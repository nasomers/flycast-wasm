# Build artifacts (large binaries — tracked in releases, not git)
build/flycast_libretro_emscripten.a
build/flycast_stubs.o
build/flycast_stubs_cpp.o
build/gles_patched.o
dist/

# Demo runtime (user provides these locally)
demo/data/
demo/bios/
demo/roms/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Internal docs (not public)
CLAUDE.md
flycast-wasm-sharing-guide.md

# Upstream port — source clone and build artifacts (huge)
upstream/source/
upstream/*.js
!upstream/flycast-wasm-test.js
upstream/*.wasm
upstream/*.data
upstream/*.log
upstream/*.txt
upstream/*.png
upstream/*.json
upstream_pkg/
flycast_libretro_upstream.*
UPSTREAM_PORT_LOG.md

# Editor
.vscode/
.idea/
*.swp
*.swo
